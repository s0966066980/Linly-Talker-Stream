---
status: accepted
---

# 由後端串流 Silero 判定使用者發話邊界

免按對話將可收音期間的麥克風音訊透過既有會話的雙向 WebRTC 音軌串流至後端，由 Silero 即時判定使用者發話的開始與結束，並作為唯一具權威性的端點偵測結果。WebRTC 負責承載整體即時串流，不使用 WebRTC VAD；瀏覽器端 RMS 可以保留作音量顯示，但不得切段或觸發 ASR。這取代目前「前端 RMS 先切錄音檔、後端 Silero 再驗證」的雙重且名實不符流程。
